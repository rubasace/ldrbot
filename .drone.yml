---
kind: pipeline
type: kubernetes
name: default

steps:
  - name: build
    image: eclipse-temurin:21-jdk
    commands:
      - apt-get update && apt-get install -y tesseract-ocr
      - mvn package -Dtesseract.data-path=/drone/src/tessdata -Dtesseract.lib-path=/usr/lib/x86_64-linux-gnu
    volumes:
      - name: m2-cache
        path: /root/.m2/repository

volumes:
  - name: m2-cache
    host:
      path: /var/lib/cache/.m2
trigger:
  event:
    - push
    - promote
hmac:
---
kind: signature
hmac: 6c8b214c7f69de46a7b26ddf50d0a6d4599d6806e580aacab21800472fa5d98a

...
